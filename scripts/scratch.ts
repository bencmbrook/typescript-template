/**
 * Usage:
 * pnpm script ./scripts/scratch.ts
 *
 * Expects `.env.local`, (run `touch .env.local` to create it)
 */
console.log("This is an example. Scripts aren't included in the final build.");
